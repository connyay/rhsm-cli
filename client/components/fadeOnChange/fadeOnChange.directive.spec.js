'use strict';

describe('Directive: fadeOnChange', function() {

  // load the directive's module
  beforeEach(module('rhsmCliApp'));

  var element,
    scope;

  beforeEach(inject(function($rootScope) {
    scope = $rootScope.$new();
  }));

});
